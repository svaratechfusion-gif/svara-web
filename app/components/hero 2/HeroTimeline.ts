// Step 7 (Motion) implements the hero's idle timeline here — breathing
// scale (0.995–1.005 over 8s) and slow Y-rotation (max 8° over 20s).
// Intentionally empty until that step; not imported anywhere yet.

export {}
