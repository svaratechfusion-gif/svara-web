---
tags: [website, todo]
updated: 2026-07-13
---

# Blog — `/blog`

`app/pages/blog.vue`. `#todo` — content pipeline not yet built. The root `content/blog/`
folder exists but only contains a `.gitkeep` (no `@nuxt/content` module is installed in
`package.json`, so that folder is currently inert scaffold, not a live CMS source — see
[[Folder Structure]]). Decide the actual content source (hand-written `lib/content/` files
matching the rest of the site, vs. adopting `@nuxt/content`, vs. a headless CMS) before
building this page out further.

## Related

[[Folder Structure]] · [[Home Content Bible]]
