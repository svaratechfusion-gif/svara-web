---
tags: [content, wip]
updated: 2026-07-13
---

# Ecosystem Content Bible

The connected-ecosystem narrative (inputs → intelligence layer → outputs) is currently
sourced from `homeSection02` in [[Home Content Bible]] (`lib/content/home.ts`) — there is no
separate dedicated `lib/content/ecosystem.ts` file today. The [[Ecosystem]] page (`/ecosystem`)
reads the same `paragraphs` array the homepage's `SolutionSection.vue` trims down via
`teaser`.

`#todo` — if `/ecosystem` grows content that doesn't belong on the homepage at all (not just a
fuller version of the same section), it should get its own `lib/content/ecosystem.ts` rather
than continuing to stretch `homeSection02`. Revisit when that page gets its own real build-out
pass.

## Related

[[Ecosystem]] · [[Home Content Bible]]
